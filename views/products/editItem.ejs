<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>
    <link rel="stylesheet" href="/edit.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet">
</head>

<body>
    <div class="container">
        <h1>Updating Purchased Item</h1>

        <form action="/product/<%=component._id%>?_method=PUT" method="POST">
            <div class="info">
                <div>
                    <label for="Item">Item: </label>
                    <input type="text" id='Item' name="item" placeholder="pc-parts" value="<%= component.item%>">
                </div>

                <div>
                    <label for="cost">Price: </label>
                    <input type="number" id="cost" name="price" placeholder="cost-of-an-item"
                        value="<%= component.price%>">
                </div>

                <div>
                    <label for="category">Category: </label>
                    <select name="category" id="category">
                        <!-- <option value="Hardware" <%= component.category === 'Hardware' ? 'selected' : '' %> >Hardware</option>
                <option value="Software" <%= component.category === 'Software' ? 'selected' : '' %> >Software</option>
                <option value="Electronics" <%= component.category === 'Electronics' ? 'selected' : '' %> >Electronics</option> -->

                        <!-- Applying changes in dynamic manner -->
                        <% for(let opt of categories){%>
                            <option value="<%=opt%>" <%=component.category===opt ? 'selected' : '' %> ><%=opt%>
                            </option>
                            <% } %>
                    </select>
                </div>
            </div>
            <button class="submit">submit</button>
            <a class="cancel" href="/product/<%= component._id%>">Abort</a>
        </form>
    </div>
</body>

</html>